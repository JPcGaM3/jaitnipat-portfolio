# Repository Agent Instructions

- Treat `PLAN.md` as the authoritative project plan for implementing the portfolio application.
- When adding or modifying code, make sure the changes align with the structure, technologies, and practices described in `PLAN.md`.
- Update `PLAN.md` if architectural decisions change.
- Documentation updates should remain in Markdown files unless a user explicitly requests another format.
